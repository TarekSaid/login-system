spring:
  profiles:
    active: development
---
server:
  port: 8181
spring:
  profiles: development
  application:
    name: user-service
security:
  user:
    password: password
---
spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
security:
  user:
    name: user
    password: password